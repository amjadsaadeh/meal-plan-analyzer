---
trigger: always_on
---

- This project uses `uv` for dependency management and environment handling.
- Always prefix python-related commands with `uv run` (e.g., `uv run python manage.py`, `uv run pytest`).
- Use `uv add` for installing new dependencies.
- Ensure all work adheres to the `uv` workflow.