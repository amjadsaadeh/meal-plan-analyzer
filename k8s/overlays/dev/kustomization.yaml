apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: meal-plan-analyzer-dev

resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml

patches:
  - path: resource-patch.yaml
