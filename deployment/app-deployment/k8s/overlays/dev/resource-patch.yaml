apiVersion: apps/v1
kind: Deployment
metadata:
  name: meal-plan-analyzer
spec:
  template:
    spec:
      containers:
        - name: meal-plan-analyzer
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "256Mi"
        - name: worker
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "256Mi"
