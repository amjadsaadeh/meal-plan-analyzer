# Dev-specific overrides for the CNPG Cluster.
# Currently a placeholder — the size mirrors the base value.
# Use this patch to add dev-specific settings such as:
#   - Smaller resource limits
#   - Backup disabled (spec.backup removed)
#   - Single-instance tuning
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: meal-plan-analyzer-db
spec:
  storage:
    size: 2Gi
