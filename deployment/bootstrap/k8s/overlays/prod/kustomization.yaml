apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: meal-plan-analyzer

resources:
  - namespace.yaml
  - ../../base

secretGenerator:
  - name: meal-plan-analyzer-secret
    envs:
      - secrets.env

generatorOptions:
  disableNameSuffixHash: true
